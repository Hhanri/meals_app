import 'package:flutter/material.dart';

class CategoryMealsView extends StatelessWidget {
  const CategoryMealsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
